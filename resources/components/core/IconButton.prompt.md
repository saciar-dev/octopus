Small circular icon-only button for kiosk navigation controls: back, forward, refresh/reset, and play. Seen bottom-right of every Speaker Preview Manager screen as a floating back+reset pair.

```jsx
<IconButton icon="back" />
<IconButton icon="refresh" variant="navy" />
```
