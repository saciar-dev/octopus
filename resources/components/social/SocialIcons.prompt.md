Row of speaker social links, shown under a speaker's name on the speaker detail screen. Icons are copied brand glyphs (Facebook, Instagram, LinkedIn, Twitter), not hand-drawn — pass `basePath` so the img src resolves from wherever the consumer mounts it.

```jsx
<SocialIcons networks={['facebook','linkedin','twitter']} basePath="../../" />
```
